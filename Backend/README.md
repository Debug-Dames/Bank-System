# Backend (OpenBank API)

Node.js + Express REST API for the OpenBank simulation.

## Requirements

- Node.js 18+
- npm 9+

## Setup

```bash
cd backend
npm install
cp .env.example .env
npm run dev
```

## Endpoints

- `GET /health` — service health check

